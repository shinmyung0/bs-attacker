#!/usr/bin/env node

const { httpServer } = require("../app");
const port = process.env.PORT || 9000;

httpServer.listen(port, () => {
  console.log("attacker server is binding on port " + port);
});
